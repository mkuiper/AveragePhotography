# AveragePhotography configuration. This file is sourced by Bash.

# Short, filename-safe identifier for the camera/location.
LOCATION="PiCamX"

# All runtime state and output is stored here. The default keeps it in this repo.
TOPDIR="${PROJECT_DIR}"

INTERVAL=100
MAX_DAILY_IMAGES=5000
LIGHT_LOWER=30
LIGHT_UPPER=220

# Optional features.
MOVEMENT=false
HDMI=false
EMAIL="" # Set an address only after mpack/mail delivery is configured.

# Camera command and exposure bracket. Each item must be a separate array element.
# For current Raspberry Pi OS, replace raspistill with an appropriate rpicam-still
# command and adjust its exposure option as needed.
CAMERA_COMMAND=(raspistill -q 100 -p 10,10,320,240 -hf -vf)
EXPOSURE_FLAG="-ev"
EXPOSURES=(0 6 18 -18 -24)

# Optional overrides (defaults are below):
# ARCHIVE="$TOPDIR/FrameArchive"
# ARCHIVE7="$TOPDIR/FrameArchive7day"
# ARCHIVE30="$TOPDIR/FrameArchive30day"
# ARCHIVEMOVEMENT="$TOPDIR/FrameArchiveMovement"
